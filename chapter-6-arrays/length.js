var myArray = [];
myArray.length

myArray[1000000] = true;
myArray.length  // 1000001
// myArray

numbers.length = 3

// numbers is ['zero', 'one', 'two']

numbers[numbers.length] = 'shi';

 // numebrs is ['zero', 'one', 'two', 'shi']

numbers.push('go');

// numebrs is ['zero', 'one', 'two', 'shi', 'go']


